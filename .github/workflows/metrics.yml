name: 📈 GitHub Metrics

on:
  schedule:
    - cron: "0 4 * * *" # Щодня о 4:00 ранку UTC
  workflow_dispatch: # Можна запускати вручну з GitHub UI

permissions:
  contents: write

jobs:
  github-metrics:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Generate metrics.svg
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.GH_STATS_TOKEN }} # Додай цей токен у Settings → Secrets
          filename: metrics.svg
          output_action: file
          output_dir: ./
          base: activity, community, repositories, metadata
          config_timezone: Europe/Kyiv

      - name: Commit & Push
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add metrics.svg
          git commit -m "📊 Update GitHub metrics" || echo "No changes"
          git push
